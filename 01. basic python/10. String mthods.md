see length of a string
```python
result = len(name)
print(result)
```


find the index of char in a string
```python
print(name.find('u'))
```


find the index of char in a string search from last
```python
print(name.rfind(" "))
```


capital the first letter of the string
```python
print(name.capitalize())
```

```python
print(myname.title())
```

make all char cap and small
```python
print(name.upper())
print(myname.lower())
```

check digit only
```python
print(name.isdigit())
```

check char only
```python
print(name.isalpha())
```

count char
```python
print(name.count('s'))
```

replace char
```python
print(name.replace('s','u'))
```

```js
text = "   hello world   "
cleaned = text.strip()
print(cleaned)  # Output: "hello world"
```


